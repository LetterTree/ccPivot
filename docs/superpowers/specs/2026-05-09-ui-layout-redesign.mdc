# 配置工具 UI 布局重构设计

日期: 2026-05-09 | 状态: 已确认

## 目标

重构 `config_switcher.py` 的 GUI 布局，从当前两个 LabelFrame 垂直堆叠改为左右分栏结构，改善视觉清晰度和操作便利性。

## 当前问题

- 供应商管理 LabelFrame 信息密度过高：下拉框 + CRUD 按钮 + 应用按钮 + 状态标签全部挤在一起
- 应用按钮（应用到两端/仅 Windows/仅 WSL）缩进在 dropdown 下方，位置不协调
- 两个 LabelFrame 垂直堆叠，缺乏视觉节奏
- 右侧大片空白未利用
- Combobox 下拉选择无法一览所有供应商

## 新布局结构

```
┌─────────────────────────────────────────────────┐
│  ccPivot                      [刷新配置] │
│  统一管理 Codex / Claude 供应商配置...          │
├─────────────────────────────────────────────────┤
│ [Codex 配置]  [Claude 配置]                     │
│                                                 │
│ ┌── 左侧 220px ──┐ ┌── 右侧弹性 ──────────────┐│
│ │ 📦 供应商       │ │ 🔗 连接配置              ││
│ │                 │ │                          ││
│ │ ● openai  ✓已应用│ │ Base URL [____________] ││
│ │   azure    ✓已应用│ │ Model    [____________] ││
│ │   local-llm      │ │ API Key  [____________] ││
│ │   deepseek       │ │                  □ 显示 ││
│ │                 │ │           [?帮助] [💾保存]││
│ │ [+新增] [🗑删除] │ │ ──────────────────────── ││
│ │                 │ │ 🚀 同步到配置文件         ││
│ │                 │ │ [两端同步][仅Win][仅WSL]  ││
│ │                 │ │ ✓ 当前已同步: openai     ││
│ └─────────────────┘ └───────────────────────────┘│
│ 就绪                                             │
└─────────────────────────────────────────────────┘
```

## 详细设计

### 左侧面板 (固定 220px)

- 标题行："📦 供应商" + 供应商数量统计
- 供应商列表：使用 Frame 容器 + 内部 Label 模拟列表项（非 ttk.Treeview，保持简洁）
  - 每行显示：供应商名称 + 状态标签
  - 选中行：蓝色背景 (`#e0f2fe`)，右侧 "▶" 指示符
  - "已应用"标记：绿色小标签 (`#dcfce7`)，所有已配置到文件的供应商都显示
  - 未配置的供应商（仅存在于 providers.json 中）：灰色文字
- 底部操作栏：[＋ 新增] [🗑 删除]

### 右侧面板 (弹性宽度)

**上半部 — 连接配置：**
- Base URL / Model / API Key 三行网格布局
- 标签宽度 75px，输入框弹性扩展
- API Key 行右侧带 "显示" checkbox
- 右下角：[? 帮助] [💾 保存]

**分隔线**

**下半部 — 同步到配置文件：**
- 三个应用按钮：[两端同步] [仅 Windows] [仅 WSL]
- "两端同步"为主操作，视觉上突出（浅蓝色背景）
- 状态行："✓ 当前已同步: xxx (两端)" 或 "(仅 Windows)" 等

### 交互规则

| 操作 | 行为 |
|------|------|
| 点击左侧列表项 | 右侧表单加载对应供应商的 Base URL / Model / API Key |
| 点击「保存」 | 将右侧输入存入供应商定义（更新 providers.json 和 model_providers） |
| 点击「两端同步」 | 先保存，再写入 config.toml + auth.json 并同步 WSL |
| 点击「仅 Windows」 | 同上，只写到 Windows 侧 |
| 点击「仅 WSL」 | 同上，只写到 WSL 侧 |
| 左侧「已应用」标记 | 所有曾在 config.toml model_providers 中有定义的供应商都显示 |

### 窗口尺寸

| 参数 | 当前 | 改为 |
|------|------|------|
| geometry | 900×520 | 820×460 |
| minsize | 760×500 | 760×420 |

### 主题风格

- 保持 ttkbootstrap 可选支持
- 无 ttkbootstrap 时 fallback 到 clam 主题
- 色系保持一致：背景 `#f6f8fb`，蓝色高亮 `#0369a1`，绿色状态 `#047857`

## 实现范围

### 改动文件

- `config_switcher.py` — `setup_codex_tab()` 和 `setup_claude_tab()` 两个方法完全重写

### 不变部分

- 所有业务逻辑方法不变（CRUD、配置读写、WSL 同步等）
- `setup_ui()` 主框架不变（header、notebook、status_bar）
- `_setup_theme()` 样式配置不变
- Codex 和 Claude 标签页对称设计，结构一致

### 不涉及

- 功能行为变更
- 新增配置项
- tests/、docs/ 等未跟踪文件
